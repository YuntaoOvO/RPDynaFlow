name: rpdynaflow
channels:
  - conda-forge
dependencies:
  - python=3.11
  # core numerics / learning (GPU build; swap to plain "pytorch" for CPU-only)
  - pytorch-gpu
  - numpy>=1.24
  - pandas>=2.0
  - scipy>=1.10
  - scikit-learn>=1.3
  - matplotlib>=3.7
  - tqdm
  # structural parsing
  - mdanalysis>=2.6
  - mdtraj>=1.9
  # optional: GROMACS interaction for training-data generation (stages 01-05)
  # - pexpect

